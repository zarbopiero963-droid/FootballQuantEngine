[pytest]
testpaths = tests test quant/tests
markers =
    windows: tests intended to run on Windows
    integration: tests that call external services (skipped by default in CI)
    slow: tests that take more than 5 seconds

