AUTO_BACKUP_BEFORE_RUN

CREA_CARTELLA export
CREA_CARTELLA notifications
CREA_CARTELLA tests

APPEND requirements.txt
openpyxl
EOF

CREA_FILE export/__init__.py
# export package
EOF

CREA_FILE export/csv_exporter.py
import csv


class CsvExporter:

    def export_value_bets(self, filepath, value_bets):

        fieldnames = ["match_id", "market", "probability", "odds"]

        with open(filepath, "w", newline="", encoding="utf-8") as f:

            writer = csv.DictWriter(f, fieldnames=fieldnames)
            writer.writeheader()

            for vb in value_bets:
                writer.writerow(
                    {
                        "match_id": vb.get("match_id"),
                        "market": vb.get("market"),
                        "probability": vb.get("probability"),
                        "odds": vb.get("odds")
                    }
                )
EOF

CREA_FILE export/excel_exporter.py
import pandas as pd


class ExcelExporter:

    def export_value_bets(self, filepath, value_bets):

        df = pd.DataFrame(value_bets)

        df.to_excel(filepath, index=False)
EOF

CREA_FILE notifications/__init__.py
# notifications package
EOF

CREA_FILE notifications/telegram_notifier.py
import requests


class TelegramNotifier:

    def __init__(self, token, chat_id):

        self.token = token
        self.chat_id = chat_id

    def send_message(self, text):

        if not self.token or not self.chat_id:
            return False

        url = f"https://api.telegram.org/bot{self.token}/sendMessage"

        payload = {"chat_id": self.chat_id, "text": text}

        response = requests.post(url, json=payload, timeout=15)

        return response.status_code == 200
EOF

CREA_FILE tests/__init__.py
# tests package
EOF

CREA_FILE tests/test_edge_detector.py
from strategies.edge_detector import EdgeDetector


def test_edge_detector_basic():

    ed = EdgeDetector()

    edge = ed.calculate_edge(0.6, 2.0)

    assert edge == 0.6 - 0.5

    assert ed.is_value_bet(0.6, 2.0) in (True, False)
EOF

CREA_FILE tests/test_poisson_model.py
from models.poisson_model import PoissonModel


def test_poisson_probabilities_sum_close_to_one():

    model = PoissonModel()

    probs = model.predict(
        home_attack=1.4,
        home_defense=1.0,
        away_attack=1.2,
        away_defense=1.1
    )

    total = probs["home_win"] + probs["draw"] + probs["away_win"]

    assert 0.95 <= total <= 1.05
EOF

CREA_FILE tests/test_bivariate_poisson.py
from models.bivariate_poisson import BivariatePoisson


def test_bivariate_probabilities_sum_to_one():

    model = BivariatePoisson()

    probs = model.predict(lambda_home=1.5, lambda_away=1.1, rho=0.05)

    total = probs["home_win"] + probs["draw"] + probs["away_win"]

    assert 0.999 <= total <= 1.001
EOF

CREA_FILE tests/test_prediction_pipeline.py
import pandas as pd

from engine.prediction_pipeline import PredictionPipeline


def test_prediction_pipeline_runs():

    features_df = pd.DataFrame(
        [
            {
                "home": "TeamA",
                "away": "TeamB",
                "home_attack": 1.4,
                "home_defense": 1.0,
                "away_attack": 1.2,
                "away_defense": 1.1,
                "elo_diff": 0
            }
        ]
    )

    odds_data = {
        "TeamA_vs_TeamB": {
            "home": 2.0,
            "draw": 3.2,
            "away": 3.8
        }
    }

    pipeline = PredictionPipeline()

    value_bets = pipeline.run(features_df, odds_data)

    assert isinstance(value_bets, list)
EOF

FIX_WHITESPACE